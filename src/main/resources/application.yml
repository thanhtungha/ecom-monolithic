server:
  port: 8080
spring:
  application:
    name: monolithic
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/TestEcom
    username: admin
    password: 123456
logging:
  file:
    name: logs/monolithic.log